import java.util.*;
public class Context {
   Map<Character,Integer> umap;
  Context() {
      umap=new HashMap<Character,Integer>();
     umap.put('a',100);
     umap.put('b',200);
     umap.put('c',1000);
  }
}
